随机森林模型常用参数及调参方法  
在机器学习领域，随机森林（Random Forest）以其高效和稳定而受到广泛欢迎。它是一种集成学习算法，通过结合多个决策树的预测结果，显著提高模型的准确性和鲁棒性。

常用参数解析 
1. 树的数量 (n_estimators)： 构成随机森林的决策树数量。更多的树通常能提升模型的效果，但会增加计算成本。 
2. 树的最大深度 (max_depth)： 限制树的深度，防止过拟合。深度越大，模型越复杂。 
3. 节点最小分裂样本数 (min_samples_split)： 节点内最少样本数，才能进一步分裂。值越大，树越浅，能有效避免过拟合。 
4. 叶子节点最小样本数 (min_samples_leaf)： 每个叶子节点最少包含的样本数，用于防止过拟合。 
5.每次分裂所考虑的特征数 (max_features)： 分裂时随机选择的特征数量。适当的选择可以增加模型的随机性，提升效果。 
6. 是否使用自助法 (bootstrap)： 决定是否对样本进行放回采样。通常选择True效果更佳。 7. 随机种子 (random_state)： 设置随机数生成器，确保结果可重复。 

超参数调优方法 
调优参数是提高随机森林模型性能的关键。
以下是三种常用的调优方法： 
1. 网格搜索（Grid Search）： 
简介：穷举所有可能的参数组合，选出最佳效果的组合。 
优缺点：找到全局最优解，但计算量大，耗时长。 
步骤：定义参数候选值，构成参数空间。 使用交叉验证搜索最优组合。 
2. 随机搜索（Random Search）： 
简介：随机采样参数组合进行训练，避免全遍历。 
优缺点：计算效率高，适用于大参数空间，但结果可能是次优解。 
步骤：设定参数取值范围。 随机采样组合进行训练和验证，控制抽样次数以平衡性能与计算开销。 
3. 贝叶斯优化（Bayesian Optimization）： 
简介：利用概率模型预测不同参数组合的表现，策略性选择参数。 
优缺点：高效且适合复杂优化问题，但实现较复杂。 
